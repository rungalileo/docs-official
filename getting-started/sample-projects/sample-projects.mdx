---
title: Sample Projects
description: Learn how to get started the the Galileo sample projects that are included in every new account
---

When you create a new Galileo account, you get a set of sample projects, pre-populated with Log streams, calculated metrics, and insights.

These projects are:

- A simple terminal-based chatbot to chat with an LLM
- A multi-agent financial services chatbot with LangGraph

You can use the sample projects to learn how to navigate Log streams, understand metrics, use insights, log your AI applications, or run experiments in code.

## View the sample projects in the Galileo console

## Run the sample projects to generate more Log streams

The full source code of the sample projects is available in the Galileo [SDK Examples GitHub repo](https://github.com/rungalileo/sdk-examples).

<CardGroup cols={2}>
<Card title="SDK Examples" icon="code" horizontal href="https://github.com/rungalileo/sdk-examples">
    Check out sample projects using Galileo
</Card>
</CardGroup>

- [Run the simple chatbot](#simple-chatbot)
- [Run the multi-agent financial services chatbot](#multi-agent-financial-services-chatbot)

### Simple chatbot

The simple chatbot is available using both the Python and TypeScript SDKs, and is available in 3 different versions for different LLMs:

- OpenAI, or any OpenAI API compatible endpoint, such as [Ollama](https://ollama.com) running locally
- Anthropic
- Azure AI Foundry models using the Azure AI inference API

#### Run the simple chatbot code

<Steps>
<Step title="Clone the SDK examples repo">
```bash Terminal
git clone https://github.com/rungalileo/sdk-examples
```
</Step>

<Step title="Navigate to the relevant project folder">

Start by navigating to the root folder by the SDK you are using:

<CodeGroup>
```bash Python
cd python/chatbot/sample-project-chatbot
```
```bash TypeScript
cd typescript/chatbot/sample-project-chatbot
```
</CodeGroup>

Then navigate to the folder for the relevant LLM you are using:

<CodeGroup>
```bash OpenAI
cd openai-ollama
```
```bash Anthropic
cd anthropic
```
```bash Azure AI Inference
cd azure-inference
```
</CodeGroup>
</Step>

<Step title="Install required dependencies">

From the project folder, Install the required dependencies. For Python, make sure to create and activate a virtual environment before installing the dependencies.

<CodeGroup>
```bash Python
pip install -r requirements.txt
```
```bash TypeScript
npm install
```
</CodeGroup>

</Step>

<Step title="Configure environment variables">
In each project folder is a `.env.example` file. Rename this file to `.env` and populate the Galileo values:

| Environment Variable | Value |
| :------------------- | :---- |
| `GALILEO_API_KEY`    | Your API key |
| `GALILEO_PROJECT`    | The name of your Galileo project |
| `GALILEO_LOG_STREAM` | The name of your Log stream |

<Note>
You can find these values from the project page for the simple chatbot sample page in the Galileo Console.
</Note>

Next populate the values for your LLM:

<Tabs>
<Tab title="OpenAI">
| Environment Variable | Value |
| :------------------- | :---- |
| `OPENAI_API_KEY`     | Your OpenAI API key. If you are using Ollama, set this to `ollama`. |
| `OPENAI_BASE_URL`    | Optional. The base URL of your OpenAI deployment. Leave this commented out if you are using the default OpenAI API. If you are using Ollama, set this to `http://localhost:11434/v1`. |
| `MODEL_NAME`         | The name of the model you are using |
</Tab>
<Tab title="Anthropic">
| Environment Variable | Value |
| :------------------- | :---- |
| `ANTHROPIC_API_KEY`  | Your Anthropic API key |
| `MODEL_NAME`         | The name of the model you are using |
</Tab>
<Tab title="Azure AI Inference">
| Environment Variable.         | Value |
| :---------------------------- | :---- |
| `AZURE_AI_INFERENCE_ENDPOINT` | Your Azure AI Foundry endpoint |
| `AZURE_AI_INFERENCE_API_KEY`  | Your Azure AI Foundry API key |
| `MODEL_NAME`                  | The name of the model deployment you are using |
</Tab>
</Tabs>

</Step>

<Step title="Run the project">
Run the project with the following command:

<CodeGroup>
```bash Python
python app.py
```
```bash TypeScript
npx tsx app.ts
```
</CodeGroup>

The app will run in your terminal, and you can ask the LLM questions and get responses:

<CodeGroup>
```output Terminal
You: Which are the Galilean moons?
The Galilean moons are the four largest moons of Jupiter, discovered by
Galileo Galilei in 1610. They are:

1. **Io** - The innermost moon, known for its intense volcanic activity
   and numerous volcanoes.
2. **Europa** - Notable for its smooth icy surface, which is believed
   to cover an ocean of liquid water beneath, making it a subject of
   interest for the search for extraterrestrial life.
3. **Ganymede** - The largest moon in the solar system, larger than the
   planet Mercury, and has its own magnetic field.
4. **Callisto** - The most heavily cratered body in the solar system,
   it is an ancient moon that has remained relatively unchanged over
   billions of years.

These moons are significant for their unique geological features and
potential for supporting life.
```
</CodeGroup>

</Step>
</Steps>

#### See the chatbot traces

#### Review the code



#### Improve the chatbot

### Multi-agent financial services chatbot