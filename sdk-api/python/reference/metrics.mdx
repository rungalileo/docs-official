---
title: metrics
---

{/* This page is autogenerated from the Python SDK. Do not edit this file as it will be replaced on the next SDK publish */}


## Metrics Objects

```python
class Metrics(BaseClientModel)
```

#### create\_custom\_llm\_metric

```python
def create_custom_llm_metric(
        name: str,
        user_prompt: str,
        node_level: StepType = StepType.llm,
        cot_enabled: bool = True,
        model_name: str = "GPT-4o",
        num_judges: int = 3,
        description: str = "",
        tags: list[str] = []) -> BaseScorerVersionResponse
```

Create a custom LLM metric.

**Arguments**:

- `name` (`str`): Name of the metric.
- `user_prompt` (`str`): User prompt for the metric.
- `node_level` (`StepType`): Node level for the metric.
- `cot_enabled` (`bool`): Whether chain-of-thought is enabled.
- `model_name` (`str`): Model name to use.
- `str`0 (`str`1): Number of judges for the metric.
- `str`2 (`str`): Description of the metric.
- `str`4 (`str`5): Tags associated with the metric.

**Returns**:

`str`6: Response containing the created metric details.

#### create\_custom\_llm\_metric

```python
def create_custom_llm_metric(
        name: str,
        user_prompt: str,
        node_level: StepType = StepType.llm,
        cot_enabled: bool = True,
        model_name: str = "GPT-4o",
        num_judges: int = 3,
        description: str = "",
        tags: list[str] = []) -> BaseScorerVersionResponse
```

Create a custom LLM metric.

**Arguments**:

- `name` (`str`): Name of the metric.
- `user_prompt` (`str`): User prompt for the metric.
- `node_level` (`StepType`): Node level for the metric.
- `cot_enabled` (`bool`): Whether chain-of-thought is enabled.
- `model_name` (`str`): Model name to use.
- `str`0 (`str`1): Number of judges for the metric.
- `str`2 (`str`): Description of the metric.
- `str`4 (`str`5): Tags associated with the metric.

**Returns**:

`str`6: Response containing the created metric details.

