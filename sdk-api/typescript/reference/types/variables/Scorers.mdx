---
title: Scorers
---

{/** This page is autogenerated from the TypeScript SDK. Do not edit this file as it will be replaced on the next SDK publish **/}

---

# Variable: Scorers

```ts
const Scorers: object;
```

Defined in: [src/types/job.types.ts](https://github.com/rungalileo/galileo-js/blob/main/src/types/job.types.ts)

Maps canonical scorer names to their aliases.
Used for normalizing scorer names from aliases to canonical form.

## Type declaration

### action_advancement_plus

```ts
readonly action_advancement_plus: "agentic_workflow_success" = 'agentic_workflow_success';
```

### action_completion_plus

```ts
readonly action_completion_plus: "agentic_session_success" = 'agentic_session_success';
```

### chunk_attribution_utilization_luna

```ts
readonly chunk_attribution_utilization_luna: "chunk_attribution_utilization_nli" = 'chunk_attribution_utilization_nli';
```

### chunk_attribution_utilization_plus

```ts
readonly chunk_attribution_utilization_plus: "chunk_attribution_utilization_gpt" = 'chunk_attribution_utilization_gpt';
```

### completeness_luna

```ts
readonly completeness_luna: "completeness_nli" = 'completeness_nli';
```

### completeness_plus

```ts
readonly completeness_plus: "completeness_gpt" = 'completeness_gpt';
```

### context_adherence_luna

```ts
readonly context_adherence_luna: "adherence_nli" = 'adherence_nli';
```

### context_adherence_plus

```ts
readonly context_adherence_plus: "groundedness" = 'groundedness';
```

### context_relevance

```ts
readonly context_relevance: "context_relevance" = 'context_relevance';
```

### correctness

```ts
readonly correctness: "factuality" = 'factuality';
```

### ground_truth_adherence_plus

```ts
readonly ground_truth_adherence_plus: "ground_truth_adherence" = 'ground_truth_adherence';
```

### input_sexist

```ts
readonly input_sexist: "input_sexist" = 'input_sexist';
```

### input_sexist_plus

```ts
readonly input_sexist_plus: "input_sexist_gpt" = 'input_sexist_gpt';
```

### input_toxicity

```ts
readonly input_toxicity: "input_toxicity" = 'input_toxicity';
```

### input_toxicity_plus

```ts
readonly input_toxicity_plus: "input_toxicity_gpt" = 'input_toxicity_gpt';
```

### instruction_adherence_plus

```ts
readonly instruction_adherence_plus: "instruction_adherence" = 'instruction_adherence';
```

### pii

```ts
readonly pii: "pii" = 'pii';
```

### prompt_injection

```ts
readonly prompt_injection: "prompt_injection" = 'prompt_injection';
```

### prompt_injection_plus

```ts
readonly prompt_injection_plus: "prompt_injection_gpt" = 'prompt_injection_gpt';
```

### prompt_perplexity

```ts
readonly prompt_perplexity: "prompt_perplexity" = 'prompt_perplexity';
```

### sexist

```ts
readonly sexist: "sexist" = 'sexist';
```

### sexist_plus

```ts
readonly sexist_plus: "sexist_gpt" = 'sexist_gpt';
```

### tone

```ts
readonly tone: "tone" = 'tone';
```

### tool_errors_plus

```ts
readonly tool_errors_plus: "tool_error_rate" = 'tool_error_rate';
```

### tool_selection_quality_plus

```ts
readonly tool_selection_quality_plus: "tool_selection_quality" = 'tool_selection_quality';
```

### toxicity

```ts
readonly toxicity: "toxicity" = 'toxicity';
```

### toxicity_plus

```ts
readonly toxicity_plus: "toxicity_gpt" = 'toxicity_gpt';
```
