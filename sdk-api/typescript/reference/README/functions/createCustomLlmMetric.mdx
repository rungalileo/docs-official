---
title: createCustomLlmMetric
---

{/** This page is autogenerated from the TypeScript SDK. Do not edit this file as it will be replaced on the next SDK publish **/}

---

# Function: createCustomLlmMetric()

```ts
function createCustomLlmMetric(
  name: string,
  userPrompt: string,
  nodeLevel: StepType,
  cotEnabled: boolean,
  modelName: string,
  numJudges: number,
  description: string,
  tags: string[],
): Promise<{
  generated_scorer?: null | {
    chain_poll_template: {
      explanation_field_name?: string;
      metric_description?: null | string;
      metric_few_shot_examples?: object[];
      metric_system_prompt?: null | string;
      template: string;
      value_field_name?: string;
    };
    id: string;
    instructions: string;
    name: string;
  };
  id: string;
  model_name?: null | string;
  num_judges?: null | number;
  registered_scorer?: null | {
    id: string;
    name: string;
  };
  version: number;
}>;
```

Defined in: [src/utils/metrics.ts:21](https://github.com/rungalileo/galileo-js/blob/main/src/utils/metrics.ts#L21)

Creates a custom LLM metric.

## Parameters

### name

`string`

The name of the custom metric.

### userPrompt

`string`

### nodeLevel

[`StepType`](/sdk-api/typescript/reference/types/enumerations/StepType) = `StepType.llm`

### cotEnabled

`boolean` = `true`

### modelName

`string` = `'GPT-4o'`

(Optional) The model name to use. Defaults to 'GPT-4o'.

### numJudges

`number` = `3`

(Optional) The number of judges to use. Defaults to 3.

### description

`string` = `''`

(Optional) A description for the metric.

### tags

`string`[] = `[]`

(Optional) Tags to associate with the metric.

## Returns

`Promise`\<\{
`generated_scorer?`: \| `null`
\| \{
`chain_poll_template`: \{
`explanation_field_name?`: `string`;
`metric_description?`: `null` \| `string`;
`metric_few_shot_examples?`: `object`[];
`metric_system_prompt?`: `null` \| `string`;
`template`: `string`;
`value_field_name?`: `string`;
\};
`id`: `string`;
`instructions`: `string`;
`name`: `string`;
\};
`id`: `string`;
`model_name?`: `null` \| `string`;
`num_judges?`: `null` \| `number`;
`registered_scorer?`: \| `null`
\| \{
`id`: `string`;
`name`: `string`;
\};
`version`: `number`;
\}\>

A promise that resolves when the metric is created.
