---
title: GalileoOTLPExporter
---

{/** This page is autogenerated from the TypeScript SDK. Do not edit this file as it will be replaced on the next SDK publish **/}

---

# Class: GalileoOTLPExporter

Defined in: [src/handlers/otel/exporter.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/exporter.ts)

OpenTelemetry OTLP span exporter preconfigured for Galileo platform integration.

This exporter wraps the standard OTLPTraceExporter with Galileo-specific
configuration and authentication. It injects Galileo resource attributes
and updates HTTP headers per export batch.

For most applications, use GalileoSpanProcessor instead, which provides
a complete tracing solution including this exporter.

## Example

```typescript
import { GalileoOTLPExporter } from "galileo";
import { BatchSpanProcessor } from "@opentelemetry/sdk-trace-base";

const exporter = new GalileoOTLPExporter({ project: "my-project" });
const processor = new BatchSpanProcessor(exporter);
```

## Implements

- `SpanExporterLike`

## Constructors

### Constructor

```ts
new GalileoOTLPExporter(config?: GalileoOTLPExporterConfig): GalileoOTLPExporter;
```

Defined in: [src/handlers/otel/exporter.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/exporter.ts)

#### Parameters

##### config?

[`GalileoOTLPExporterConfig`](/sdk-api/typescript/reference/README/interfaces/GalileoOTLPExporterConfig)

#### Returns

`GalileoOTLPExporter`

## Properties

### logstream

```ts
readonly logstream: string;
```

Defined in: [src/handlers/otel/exporter.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/exporter.ts)

---

### project

```ts
readonly project: string;
```

Defined in: [src/handlers/otel/exporter.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/exporter.ts)

## Methods

### export()

```ts
export(spans: ReadableSpanLike[], resultCallback: (result: ExportResultLike) => void): void;
```

Defined in: [src/handlers/otel/exporter.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/exporter.ts)

Export spans to Galileo, injecting resource attributes from span attributes.

For each span, reads galileo.\* attributes (set by GalileoSpanProcessor.onStart)
and merges them into the span's resource. Also updates HTTP headers per batch.

#### Parameters

##### spans

`ReadableSpanLike`[]

##### resultCallback

(`result`: `ExportResultLike`) => `void`

#### Returns

`void`

#### Implementation of

```ts
SpanExporterLike.export;
```

---

### forceFlush()

```ts
forceFlush(): Promise<void>;
```

Defined in: [src/handlers/otel/exporter.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/exporter.ts)

#### Returns

`Promise`\<`void`\>

#### Implementation of

```ts
SpanExporterLike.forceFlush;
```

---

### shutdown()

```ts
shutdown(): Promise<void>;
```

Defined in: [src/handlers/otel/exporter.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/exporter.ts)

#### Returns

`Promise`\<`void`\>

#### Implementation of

```ts
SpanExporterLike.shutdown;
```
