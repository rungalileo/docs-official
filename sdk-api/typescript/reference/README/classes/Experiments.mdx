---
title: Experiments
---

{/** This page is autogenerated from the TypeScript SDK. Do not edit this file as it will be replaced on the next SDK publish **/}

---

# Class: Experiments

Defined in: [src/entities/experiments.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/experiments.ts)

Entity class for managing experiments.
Provides high-level methods for creating, getting, updating, deleting, and querying experiments.

## Constructors

### Constructor

```ts
new Experiments(): Experiments;
```

#### Returns

`Experiments`

## Methods

### createExperiment()

```ts
createExperiment(options: object): Promise<ExperimentResponseType>;
```

Defined in: [src/entities/experiments.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/experiments.ts)

Creates a new experiment.

#### Parameters

##### options

The options for creating an experiment.

###### dataset?

\| `null`
\| [`ExperimentDatasetRequest`](/sdk-api/typescript/reference/types/type-aliases/ExperimentDatasetRequest)

(Optional) The dataset configuration.

###### metrics?

(
\| `string`
\| [`Metric`](/sdk-api/typescript/reference/types/interfaces/Metric)
\| [`LocalMetricConfig`](/sdk-api/typescript/reference/types/interfaces/LocalMetricConfig))[]

(Optional) List of server-side metrics to configure for the experiment.

###### name

`string`

The name of the experiment.

###### projectId?

`string`

(Optional) The unique identifier of the project.

###### projectName?

`string`

(Optional) The name of the project.

#### Returns

`Promise`\<[`ExperimentResponseType`](/sdk-api/typescript/reference/types/type-aliases/ExperimentResponseType)\>

A promise that resolves to the created experiment.

---

### deleteExperiment()

```ts
deleteExperiment(options: object): Promise<void>;
```

Defined in: [src/entities/experiments.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/experiments.ts)

Deletes an experiment.

#### Parameters

##### options

The options for deleting an experiment.

###### id

`string`

The unique identifier of the experiment.

###### projectId

`string`

The unique identifier of the project.

#### Returns

`Promise`\<`void`\>

A promise that resolves when the experiment is deleted.

---

### getExperiment()

```ts
getExperiment(options: object): Promise<
  | undefined
| ExperimentResponseType>;
```

Defined in: [src/entities/experiments.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/experiments.ts)

Gets an experiment by ID or name.

#### Parameters

##### options

The options for getting an experiment.

###### id?

`string`

(Optional) The unique identifier of the experiment.

###### name?

`string`

(Optional) The name of the experiment.

###### projectId?

`string`

(Optional) The unique identifier of the project.

###### projectName?

`string`

(Optional) The name of the project.

#### Returns

`Promise`\<
\| `undefined`
\| [`ExperimentResponseType`](/sdk-api/typescript/reference/types/type-aliases/ExperimentResponseType)\>

A promise that resolves to the experiment, or undefined if not found.

---

### getExperimentColumns()

```ts
getExperimentColumns(options: object): Promise<ExperimentsAvailableColumnsResponse>;
```

Defined in: [src/entities/experiments.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/experiments.ts)

Returns all available columns for the experiment comparison table.

Scorer-backed metric columns have IDs of the form `"metrics/{scorer-uuid}"`, which maps
directly to the keys in `experiment.metricAggregates`. Use `column.metricKeyAlias` for
the legacy snake_case metric name and `column.label` for the display label.

#### Parameters

##### options

###### projectId?

`string`

###### projectName?

`string`

#### Returns

`Promise`\<[`ExperimentsAvailableColumnsResponse`](/sdk-api/typescript/reference/types/type-aliases/ExperimentsAvailableColumnsResponse)\>

#### Example

```ts
const cols = await experiments.getExperimentColumns({
  projectName: "My Project",
});
const colMap = Object.fromEntries((cols.columns ?? []).map((c) => [c.id, c]));
for (const [metricId, agg] of Object.entries(
  experiment.metricAggregates ?? {},
)) {
  const col = colMap[`metrics/${metricId}`]; // null metricKeyAlias for system metrics
  const label = col?.label ?? metricId;
  console.log(`${label}: avg=${agg.avg}`);
}
```

---

### getExperiments()

```ts
getExperiments(options?: object): Promise<ExperimentResponseType[]>;
```

Defined in: [src/entities/experiments.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/experiments.ts)

Gets experiments for a project.

#### Parameters

##### options?

(Optional) The options for getting experiments.

###### projectId?

`string`

(Optional) The unique identifier of the project.

###### projectName?

`string`

(Optional) The name of the project.

#### Returns

`Promise`\<[`ExperimentResponseType`](/sdk-api/typescript/reference/types/type-aliases/ExperimentResponseType)[]\>

A promise that resolves to an array of experiments.

---

### runExperiment()

```ts
runExperiment<T>(params: RunExperimentParams<T>): Promise<RunExperimentOutput>;
```

Defined in: [src/entities/experiments.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/experiments.ts)

Runs an experiment by processing each row of a dataset through a specified function or prompt template.
If metrics are provided, they will be used to evaluate the experiment.

#### Type Parameters

##### T

`T` _extends_ `Record`\<`string`, `unknown`\>

#### Parameters

##### params

[`RunExperimentParams`](/sdk-api/typescript/reference/types/type-aliases/RunExperimentParams)\<`T`\>

The experiment run parameters.

#### Returns

`Promise`\<[`RunExperimentOutput`](/sdk-api/typescript/reference/types/type-aliases/RunExperimentOutput)\>

A promise that resolves to the experiment run output.

---

### runExperimentWithFunction()

```ts
runExperimentWithFunction<T>(
   experiment: ExperimentResponseType,
   projectName: string,
   dataset: DatasetRecord[],
   processFn: (input: T, metadata?: Record<string, unknown>) => Promise<unknown>,
   localMetrics: LocalMetricConfig[],
projectId?: string): Promise<RunExperimentWithFunctionOutput>;
```

Defined in: [src/entities/experiments.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/experiments.ts)

Runs an experiment by processing each row of a dataset using a runner function.

#### Type Parameters

##### T

`T` _extends_ `Record`\<`string`, `unknown`\>

#### Parameters

##### experiment

[`ExperimentResponseType`](/sdk-api/typescript/reference/types/type-aliases/ExperimentResponseType)

The experiment to run.

##### projectName

`string`

The name of the project.

##### dataset

[`DatasetRecord`](/sdk-api/typescript/reference/types/interfaces/DatasetRecord)[]

The content of the dataset.

##### processFn

(`input`: `T`, `metadata?`: `Record`\<`string`, `unknown`\>) => `Promise`\<`unknown`\>

The runner function to use for processing.

##### localMetrics

[`LocalMetricConfig`](/sdk-api/typescript/reference/types/interfaces/LocalMetricConfig)[] = `[]`

(Optional) The local metrics to use for client-side evaluation.

##### projectId?

`string`

(Optional) The unique identifier of the project. If not provided, a dummy value will be used for link generation.

#### Returns

`Promise`\<[`RunExperimentWithFunctionOutput`](/sdk-api/typescript/reference/types/type-aliases/RunExperimentWithFunctionOutput)\>

A promise that resolves to the experiment run output.

---

### updateExperiment()

```ts
updateExperiment(options: object): Promise<ExperimentResponseType>;
```

Defined in: [src/entities/experiments.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/experiments.ts)

Updates an experiment.

#### Parameters

##### options

The options for updating an experiment.

###### id

`string`

The unique identifier of the experiment.

###### projectId

`string`

The unique identifier of the project.

###### updateRequest

[`ExperimentUpdateRequest`](/sdk-api/typescript/reference/types/type-aliases/ExperimentUpdateRequest)

The experiment update request.

#### Returns

`Promise`\<[`ExperimentResponseType`](/sdk-api/typescript/reference/types/type-aliases/ExperimentResponseType)\>

A promise that resolves to the updated experiment.
