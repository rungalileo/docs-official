---
title: ExperimentTags
---

{/** This page is autogenerated from the TypeScript SDK. Do not edit this file as it will be replaced on the next SDK publish **/}

---

# Class: ExperimentTags

Defined in: [src/entities/experiment-tags.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/experiment-tags.ts)

Entity class for managing experiment tags.
Provides high-level methods for getting, creating, updating, and deleting experiment tags.

## Constructors

### Constructor

```ts
new ExperimentTags(): ExperimentTags;
```

#### Returns

`ExperimentTags`

## Methods

### deleteExperimentTag()

```ts
deleteExperimentTag(options: object): Promise<void>;
```

Defined in: [src/entities/experiment-tags.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/experiment-tags.ts)

Deletes a tag for a specific experiment.

#### Parameters

##### options

The options for deleting an experiment tag.

###### experimentId

`string`

The unique identifier of the experiment.

###### projectId

`string`

The unique identifier of the project.

###### tagId

`string`

The unique identifier of the tag to delete.

#### Returns

`Promise`\<`void`\>

A promise that resolves when the tag is deleted.

---

### getExperimentTags()

```ts
getExperimentTags(options: object): Promise<RunTagDB[]>;
```

Defined in: [src/entities/experiment-tags.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/experiment-tags.ts)

Gets all tags for a specific experiment.

#### Parameters

##### options

The options for getting experiment tags.

###### experimentId

`string`

The unique identifier of the experiment.

###### projectId

`string`

The unique identifier of the project.

#### Returns

`Promise`\<[`RunTagDB`](/sdk-api/typescript/reference/types/type-aliases/RunTagDB)[]\>

A promise that resolves to an array of experiment tags.

---

### upsertExperimentTag()

```ts
upsertExperimentTag(options: object): Promise<RunTagDB>;
```

Defined in: [src/entities/experiment-tags.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/experiment-tags.ts)

Upserts (creates or updates) a tag for a specific experiment.

#### Parameters

##### options

The options for upserting an experiment tag.

###### experimentId

`string`

The unique identifier of the experiment.

###### key

`string`

The tag key.

###### projectId

`string`

The unique identifier of the project.

###### tagType?

`string`

(Optional) The type of tag (default: 'generic').

###### value

`string`

The tag value.

#### Returns

`Promise`\<[`RunTagDB`](/sdk-api/typescript/reference/types/type-aliases/RunTagDB)\>

A promise that resolves to the created or updated tag.
