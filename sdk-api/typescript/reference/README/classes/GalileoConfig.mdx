---
title: GalileoConfig
---

{/** This page is autogenerated from the TypeScript SDK. Do not edit this file as it will be replaced on the next SDK publish **/}

---

# Class: GalileoConfig

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:88

Singleton configuration for the Galileo SDK (env-aware across Node, Deno, and browser).

## Properties

### apiKey

```ts
readonly apiKey: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:92

---

### apiUrl

```ts
readonly apiUrl: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:91

---

### caCertContent

```ts
readonly caCertContent: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:103

---

### caCertPath

```ts
readonly caCertPath: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:102

---

### clientCertPath

```ts
readonly clientCertPath: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:104

---

### clientKeyPath

```ts
readonly clientKeyPath: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:105

---

### consoleUrl

```ts
readonly consoleUrl: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:90

---

### jwtToken

```ts
readonly jwtToken: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:97

---

### logLevel

```ts
readonly logLevel: undefined | LogLevel;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:99

---

### logStreamName

```ts
readonly logStreamName: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:101

---

### password

```ts
readonly password: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:94

---

### projectName

```ts
readonly projectName: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:100

---

### refreshToken

```ts
readonly refreshToken: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:98

---

### rejectUnauthorized

```ts
readonly rejectUnauthorized: undefined | boolean;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:106

---

### ssoIdToken

```ts
readonly ssoIdToken: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:95

---

### ssoProvider

```ts
readonly ssoProvider: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:96

---

### username

```ts
readonly username: undefined | string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:93

## Accessors

### snapshot

#### Get Signature

```ts
get snapshot(): GalileoConfigSnapshot;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:125

Returns a snapshot compatible with BaseEntity, including resolved apiUrl, apiKey, login, sso, and cert.

- apiUrl is resolved from consoleUrl if not explicitly set
- login contains username and/or password if present
- sso contains idToken and/or provider if present
- cert contains all configured TLS/certificate settings if any are present

##### Returns

`GalileoConfigSnapshot`

The config snapshot for entity authentication and API configuration.

## Methods

### getApiUrl()

```ts
getApiUrl(projectType?: string): string;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:163

Returns the API base URL, resolved from consoleUrl or explicit apiUrl.

Resolution logic:

1. If apiUrl is set, return it as-is
2. If consoleUrl is set, derive apiUrl by replacing "app.galileo.ai" or "console" with "api"
3. For localhost consoleUrl, return "http://localhost:8088"
4. If neither consoleUrl nor apiUrl is set, use projectType default (e.g., "gen_ai" → DEFAULT_API_URL)
5. If no projectType and neither URL is set, throw an error

#### Parameters

##### projectType?

`string`

(Optional) Default project type for API URL when neither apiUrl nor consoleUrl is set.

#### Returns

`string`

The resolved API URL.

#### Throws

Error if apiUrl, consoleUrl, and projectType are all unset.

---

### getAuthCredentials()

```ts
getAuthCredentials(): AuthCredentials;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:172

Returns the current authentication credentials.

Extracts and returns all present credentials: API key, username/password pair, and/or SSO tokens.
Only populated fields are included in the returned object.

#### Returns

`AuthCredentials`

The AuthCredentials object with present credential values.

---

### getCertConfig()

```ts
getCertConfig(): null | CertConfig;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:182

Returns TLS/certificate configuration for API requests.

Extracts and returns all present certificate settings: CA certificate (path or content),
client certificate and key (for mTLS), and rejectUnauthorized flag.
Only populated fields are included in the returned object.

#### Returns

`null` \| `CertConfig`

The CertConfig object with present values, or null if no certificate configuration is set.

---

### logConfig()

```ts
logConfig(): void;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:190

Logs a safe summary of the current configuration to the console.

Omits sensitive values (passwords, API keys, SSO tokens) and instead logs boolean flags
(hasApiKey, hasPassword, hasSsoIdToken) to indicate their presence without revealing content.
Useful for debugging configuration issues in production environments.

#### Returns

`void`

---

### get()

```ts
static get(overrides?: GalileoConfigInput): GalileoConfig;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:141

Returns the singleton config instance, resolving from environment and optional overrides.

On first call (or when overrides are provided), resolves configuration from:

1. Environment variables or browser storage (via resolveFromEnvironment)
2. Constructor overrides (via merge)

The instance is cached and reused on subsequent calls unless overrides are provided
or the cached instance has only URL defaults (no auth, certs, project, etc.),
which indicates environment variables may not have been loaded yet (e.g. late dotenv init).
To reset the singleton, call reset().

#### Parameters

##### overrides?

[`GalileoConfigInput`](/sdk-api/typescript/reference/README/type-aliases/GalileoConfigInput)

(Optional) Config values to merge over environment and defaults.

#### Returns

`GalileoConfig`

The GalileoConfig singleton instance.

---

### reset()

```ts
static reset(): void;
```

Defined in: node_modules/galileo-generated/dist/commonjs/lib/galileo-config.d.ts:148

Clears the singleton instance.

Next call to get() will rebuild the instance from environment variables or browser storage.
Useful for testing or when configuration has changed and needs to be reloaded.

#### Returns

`void`
