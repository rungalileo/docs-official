---
title: LogStreams
---

{/** This page is autogenerated from the TypeScript SDK. Do not edit this file as it will be replaced on the next SDK publish **/}

---

# Class: LogStreams

Defined in: [src/entities/log-streams.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/log-streams.ts)

Class for managing log streams in the Galileo platform.
Contains all implementation logic for log stream operations.

## Constructors

### Constructor

```ts
new LogStreams(): LogStreams;
```

#### Returns

`LogStreams`

## Methods

### create()

```ts
create(name: string, options?: object): Promise<LogStream>;
```

Defined in: [src/entities/log-streams.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/log-streams.ts)

Creates a new log stream.

#### Parameters

##### name

`string`

The name of the log stream.

##### options?

(Optional) The options for creating the log stream.

###### projectId?

`string`

(Optional) The ID of the project.

###### projectName?

`string`

(Optional) The name of the project.

#### Returns

`Promise`\<[`LogStream`](/sdk-api/typescript/reference/README/classes/LogStream)\>

A promise that resolves to the created log stream.

---

### enableMetrics()

```ts
enableMetrics(options: object): Promise<LocalMetricConfig[]>;
```

Defined in: [src/entities/log-streams.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/log-streams.ts)

Enables metrics for a log stream.

#### Parameters

##### options

The options for enabling metrics.

###### logStreamName?

`string`

(Optional) The name of the log stream.

###### metrics

(
\| `string`
\| [`Metric`](/sdk-api/typescript/reference/types/interfaces/Metric)
\| [`LocalMetricConfig`](/sdk-api/typescript/reference/types/interfaces/LocalMetricConfig))[]

List of metrics to enable. Can include GalileoMetrics const object values, string names, Metric objects, or LocalMetricConfig objects with scorerFn for client-side scoring.

###### projectName?

`string`

(Optional) The name of the project.

#### Returns

`Promise`\<[`LocalMetricConfig`](/sdk-api/typescript/reference/types/interfaces/LocalMetricConfig)[]\>

A promise that resolves to the list of local metric configurations that need client-side processing.

---

### get()

```ts
get(options: object): Promise<undefined | LogStream>;
```

Defined in: [src/entities/log-streams.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/log-streams.ts)

Retrieves a log stream by ID or name.

#### Parameters

##### options

The options for retrieving a log stream.

###### id?

`string`

(Optional) The ID of the log stream.

###### name?

`string`

(Optional) The name of the log stream.

###### projectId?

`string`

(Optional) The ID of the project.

###### projectName?

`string`

(Optional) The name of the project.

#### Returns

`Promise`\<`undefined` \| [`LogStream`](/sdk-api/typescript/reference/README/classes/LogStream)\>

A promise that resolves to the log stream, or undefined if not found.

---

### list()

```ts
list(options: object): Promise<LogStream[]>;
```

Defined in: [src/entities/log-streams.ts](https://github.com/rungalileo/galileo-js/blob/main/src/entities/log-streams.ts)

Lists all log streams for a project.

#### Parameters

##### options

The options for listing log streams.

###### projectId?

`string`

(Optional) The ID of the project.

###### projectName?

`string`

(Optional) The name of the project.

#### Returns

`Promise`\<[`LogStream`](/sdk-api/typescript/reference/README/classes/LogStream)[]\>

A promise that resolves to an array of log streams.
