---
title: GalileoTracingProcessor
---

{/** This page is autogenerated from the TypeScript SDK. Do not edit this file as it will be replaced on the next SDK publish **/}

---

# Class: GalileoTracingProcessor

Defined in: [src/handlers/openai-agents/index.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/openai-agents/index.ts)

GalileoTracingProcessor implements the OpenAI Agents SDK TracingProcessor interface
to capture agent runs and emit them to GalileoLogger.

Trace Input Handling:

- Trace-level input is populated from the first LLM or Tool span with non-empty input
- This ensures user queries are preserved in trace metadata
- Falls back to trace name if no meaningful input is captured

## Implements

- `TracingProcessor`

## Constructors

### Constructor

```ts
new GalileoTracingProcessor(
   galileoLogger?: GalileoLogger,
   flushOnTraceEnd?: boolean,
   ingestionHook?: (request: LogTracesIngestRequest) => void | Promise<void>): GalileoTracingProcessor;
```

Defined in: [src/handlers/openai-agents/index.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/openai-agents/index.ts)

Creates a new GalileoTracingProcessor.

#### Parameters

##### galileoLogger?

[`GalileoLogger`](/sdk-api/typescript/reference/README/classes/GalileoLogger)

(Optional) The GalileoLogger instance to use. Defaults to singleton logger.

##### flushOnTraceEnd?

`boolean` = `true`

(Optional) Whether to flush the logger after each trace ends. Defaults to true.

##### ingestionHook?

(`request`: [`LogTracesIngestRequest`](/sdk-api/typescript/reference/README/type-aliases/LogTracesIngestRequest)) => `void` \| `Promise`\<`void`\>

(Optional) Async callback invoked with the trace ingest request payload before sending to the API. When provided without a galileoLogger, a new GalileoLogger is created with this hook.

#### Returns

`GalileoTracingProcessor`

## Methods

### forceFlush()

```ts
forceFlush(): Promise<void>;
```

Defined in: [src/handlers/openai-agents/index.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/openai-agents/index.ts)

Forces a flush of any pending data.

#### Returns

`Promise`\<`void`\>

#### Implementation of

```ts
TracingProcessor.forceFlush;
```

---

### onSpanEnd()

```ts
onSpanEnd(span: AgentSpan): Promise<void>;
```

Defined in: [src/handlers/openai-agents/index.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/openai-agents/index.ts)

Called when a span ends. Finalises duration, merges data, and handles errors.

#### Parameters

##### span

`AgentSpan`

The span that ended.

#### Returns

`Promise`\<`void`\>

#### Implementation of

```ts
TracingProcessor.onSpanEnd;
```

---

### onSpanStart()

```ts
onSpanStart(span: AgentSpan): Promise<void>;
```

Defined in: [src/handlers/openai-agents/index.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/openai-agents/index.ts)

Called when a span starts. Maps span type, creates a Node, and links it to its parent.

#### Parameters

##### span

`AgentSpan`

The span that started.

#### Returns

`Promise`\<`void`\>

#### Implementation of

```ts
TracingProcessor.onSpanStart;
```

---

### onTraceEnd()

```ts
onTraceEnd(trace: AgentTrace): Promise<void>;
```

Defined in: [src/handlers/openai-agents/index.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/openai-agents/index.ts)

Called when a trace ends. Commits the span tree and optionally flushes the logger.

#### Parameters

##### trace

`AgentTrace`

The trace that ended.

#### Returns

`Promise`\<`void`\>

#### Implementation of

```ts
TracingProcessor.onTraceEnd;
```

---

### onTraceStart()

```ts
onTraceStart(trace: AgentTrace): Promise<void>;
```

Defined in: [src/handlers/openai-agents/index.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/openai-agents/index.ts)

Called when a trace starts. Creates a root agent node.

#### Parameters

##### trace

`AgentTrace`

The trace that started.

#### Returns

`Promise`\<`void`\>

#### Implementation of

```ts
TracingProcessor.onTraceStart;
```

---

### shutdown()

```ts
shutdown(timeout?: number): Promise<void>;
```

Defined in: [src/handlers/openai-agents/index.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/openai-agents/index.ts)

Shuts down the processor, flushing any pending data.

#### Parameters

##### timeout?

`number`

#### Returns

`Promise`\<`void`\>

#### Implementation of

```ts
TracingProcessor.shutdown;
```

---

### addGalileoCustomSpan()

```ts
static addGalileoCustomSpan<T>(
   galileoSpan: GalileoSpanLike,
   callback: () => T | Promise<T>,
options?: object): Promise<T>;
```

Defined in: [src/handlers/openai-agents/index.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/openai-agents/index.ts)

Runs a callback under a custom Galileo span that is registered with the OpenAI Agents SDK
trace provider and properly nested under the currently active span.

The callback is the scope of the span's lifetime — it starts when the callback starts and
ends when it returns or throws. Any SDK spans created inside the callback are automatically
nested as children of this custom span.

#### Type Parameters

##### T

`T`

#### Parameters

##### galileoSpan

`GalileoSpanLike`

Galileo span metadata (type, input, output, metadata, tags, statusCode).
Mutable — update galileoSpan.output inside the callback to capture results.

##### callback

() => `T` \| `Promise`\<`T`\>

The work to run under this span. Return value is passed through.

##### options?

###### extraData?

`Record`\<`string`, `unknown`\>

Additional data to attach to the span payload.

###### name?

`string`

Display name in Galileo. Overrides galileoSpan.name.

#### Returns

`Promise`\<`T`\>

A promise that resolves to the callback's return value.
