---
title: GalileoSpanProcessor
---

{/** This page is autogenerated from the TypeScript SDK. Do not edit this file as it will be replaced on the next SDK publish **/}

---

# Class: GalileoSpanProcessor

Defined in: [src/handlers/otel/processor.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/processor.ts)

Complete OpenTelemetry span processor with integrated Galileo export functionality.

This processor combines span processing and export capabilities into a single
component that can be directly attached to any OpenTelemetry TracerProvider.

On span start, it reads Galileo context from AsyncLocalStorage (set by `init()`,
`galileoContext`, or experiment context) and stamps galileo.\* attributes onto each span.
These attributes are then read by the GalileoOTLPExporter at export time.

## Example

```typescript
import { GalileoSpanProcessor, addGalileoSpanProcessor } from "galileo";
import { NodeTracerProvider } from "@opentelemetry/sdk-trace-node";

const provider = new NodeTracerProvider();
const processor = new GalileoSpanProcessor({ project: "my-project" });
addGalileoSpanProcessor(provider, processor);
provider.register();

const tracer = provider.getTracer("my-service");
const span = tracer.startSpan("my-operation");
span.end();
```

## Implements

- `SpanProcessorLike`

## Constructors

### Constructor

```ts
new GalileoSpanProcessor(config?: GalileoSpanProcessorConfig): GalileoSpanProcessor;
```

Defined in: [src/handlers/otel/processor.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/processor.ts)

#### Parameters

##### config?

[`GalileoSpanProcessorConfig`](/sdk-api/typescript/reference/README/interfaces/GalileoSpanProcessorConfig)

#### Returns

`GalileoSpanProcessor`

## Accessors

### exporter

#### Get Signature

```ts
get exporter(): GalileoOTLPExporter;
```

Defined in: [src/handlers/otel/processor.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/processor.ts)

##### Returns

[`GalileoOTLPExporter`](/sdk-api/typescript/reference/README/classes/GalileoOTLPExporter)

---

### processor

#### Get Signature

```ts
get processor(): SpanProcessorLike;
```

Defined in: [src/handlers/otel/processor.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/processor.ts)

##### Returns

`SpanProcessorLike`

## Methods

### forceFlush()

```ts
forceFlush(): Promise<void>;
```

Defined in: [src/handlers/otel/processor.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/processor.ts)

#### Returns

`Promise`\<`void`\>

#### Implementation of

```ts
SpanProcessorLike.forceFlush;
```

---

### onEnd()

```ts
onEnd(span: ReadableSpanLike): void;
```

Defined in: [src/handlers/otel/processor.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/processor.ts)

#### Parameters

##### span

`ReadableSpanLike`

#### Returns

`void`

#### Implementation of

```ts
SpanProcessorLike.onEnd;
```

---

### onStart()

```ts
onStart(span: SpanLike, parentContext: ContextLike): void;
```

Defined in: [src/handlers/otel/processor.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/processor.ts)

Inject Galileo context attributes onto each span at creation time.

Reads context from AsyncLocalStorage (experimentContext from singleton.ts),
falls back to constructor config, then environment variables.
Experiment ID takes priority over logstream when both are present.

#### Parameters

##### span

`SpanLike`

##### parentContext

`ContextLike`

#### Returns

`void`

#### Implementation of

```ts
SpanProcessorLike.onStart;
```

---

### shutdown()

```ts
shutdown(): Promise<void>;
```

Defined in: [src/handlers/otel/processor.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/otel/processor.ts)

#### Returns

`Promise`\<`void`\>

#### Implementation of

```ts
SpanProcessorLike.shutdown;
```
