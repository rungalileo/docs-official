---
title: galileoContext
---

{/** This page is autogenerated from the TypeScript SDK. Do not edit this file as it will be replaced on the next SDK publish **/}

---

# Variable: galileoContext

```ts
const galileoContext: object;
```

Defined in: [src/singleton.ts](https://github.com/rungalileo/galileo-js/blob/main/src/singleton.ts)

Lifecycle and context API for Galileo logging.
Groups init, flush, reset, and session methods for ergonomic use.

## Type declaration

### clearSession()

```ts
clearSession: () => void;
```

Clears the current session ID on the logger for the current context.
Subsequent traces are not associated with a session until startSession or setSession is called.

#### Returns

`void`

Nothing.

### flush()

```ts
flush: (options: LoggerKeyOptions) => Promise<void>;
```

Flushes (uploads) traces from a specific logger to the Galileo platform.

#### Parameters

##### options

`LoggerKeyOptions` = `{}`

Configuration options to identify which logger to flush

#### Returns

`Promise`\<`void`\>

A promise that resolves when the logger is flushed

### flushAll()

```ts
flushAll: () => Promise<void>;
```

Flushes (uploads) all captured traces to the Galileo platform.

#### Returns

`Promise`\<`void`\>

A promise that resolves when all loggers are flushed

### init()

```ts
init: (options: GetLoggerOptions & object) => Promise<void>;
```

Gets or creates a logger and optionally initializes a session.

Note: When startNewSession is true and an existing session is found via externalId, it is returned as-is.
All other session options (sessionName, previousSessionId, metadata) are ignored in that case -
they only apply when creating a new session. To update an existing session, use an explicit update method.

#### Parameters

##### options

`GetLoggerOptions` & `object` = `{}`

Configuration options to initialize the logger

#### Returns

`Promise`\<`void`\>

A promise that resolves when initialization is complete

### reset()

```ts
reset: (options: LoggerKeyOptions) => Promise<void>;
```

Resets (terminates and removes) a specific logger instance.

#### Parameters

##### options

`LoggerKeyOptions` = `{}`

Configuration options to identify which logger to reset

#### Returns

`Promise`\<`void`\>

A promise that resolves when the logger is reset

### resetAll()

```ts
resetAll: () => Promise<void>;
```

Resets (terminates and removes) all logger instances.

#### Returns

`Promise`\<`void`\>

A promise that resolves when all loggers are reset

### setSession()

```ts
setSession: (sessionId: string) => void;
```

Sets the session ID on the logger for the current context.
Traces created via log() are associated with this session.

#### Parameters

##### sessionId

`string`

The session ID to set.

#### Returns

`void`

Nothing.

### startSession()

```ts
startSession: (options?: StartSessionOptions) => Promise<string>;
```

Starts a new session on the logger for the current context.

#### Parameters

##### options?

[`StartSessionOptions`](/sdk-api/typescript/reference/README/interfaces/StartSessionOptions)

(Optional) Session options.

#### Returns

`Promise`\<`string`\>

A promise that resolves to the session ID.
