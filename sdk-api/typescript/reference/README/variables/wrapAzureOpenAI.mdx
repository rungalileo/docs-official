---
title: wrapAzureOpenAI
---

{/** This page is autogenerated from the TypeScript SDK. Do not edit this file as it will be replaced on the next SDK publish **/}

---

# Variable: wrapAzureOpenAI()

```ts
const wrapAzureOpenAI: <T>(openAIClient: T, logger?: GalileoLogger) => T =
  wrapOpenAI;
```

Defined in: [src/handlers/openai/index.ts](https://github.com/rungalileo/galileo-js/blob/main/src/handlers/openai/index.ts)

Wraps an Azure OpenAI instance with Galileo logging and observability.

Alias for wrapOpenAI - AzureOpenAI extends OpenAI and has the same API surface.

Wraps an OpenAI instance with logging.

## Type Parameters

### T

`T` _extends_ `OpenAIType`

## Parameters

### openAIClient

`T`

The OpenAI instance to wrap.

### logger?

[`GalileoLogger`](/sdk-api/typescript/reference/README/classes/GalileoLogger)

The logger to use. Defaults to a new GalileoLogger instance.

## Returns

`T`

The wrapped OpenAI instance.

Usage:

```typescript
import { wrapOpenAI } from "galileo";

const openai = wrapOpenAI(new OpenAI({ apiKey: process.env.OPENAI_API_KEY }));

await openai.chat.completions.create({
  model: "gpt-4o",
  messages: [{ content: "Say hello world!", role: "user" }],
});
```

## Param

The AzureOpenAI instance to wrap

## Param

Optional GalileoLogger instance. If not provided, uses the singleton instance.

## Returns

The wrapped Azure OpenAI instance
