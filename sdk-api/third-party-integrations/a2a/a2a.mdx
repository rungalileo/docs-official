---
title: Google A2A Protocol
description: Learn how to add end-to-end observability to multi-agent systems using the A2A protocol with Galileo
tag: "Python"
---

{/*<!-- markdownlint-disable MD044 -->*/}

import SnippetQuickstart from "/snippets/code/python/sdk/otel/a2a/quickstart.mdx";

{/*<!-- markdownlint-enable MD044 -->*/}

The `galileo-a2a` package provides automatic OpenTelemetry instrumentation for the [Google A2A (Agent-to-Agent)](https://github.com/google/A2A) protocol. It traces all client and server interactions across agent boundaries, giving you end-to-end distributed tracing of multi-agent systems in Galileo.

## Installation

```bash
pip install galileo-a2a
```

<Note>
**Requirements:** Python 3.10+, `a2a-sdk >= 0.3.0`, and a [Galileo API key](https://app.galileo.ai/settings/api-keys).
</Note>

## Quick start

<Steps>
<Step title="Set your environment variables">

```ini .env
GALILEO_API_KEY="your-galileo-api-key"
GALILEO_PROJECT="your-galileo-project-name"
GALILEO_LOG_STREAM="your-galileo-log-stream"
```

</Step>
<Step title="Instrument A2A">

<CodeGroup>
<SnippetQuickstart/>
</CodeGroup>

Once instrumented, all A2A interactions are automatically logged to Galileo, including client calls, server handlers, cross-agent trace context propagation, and task lifecycle tracking.

</Step>
</Steps>

## How it works

`galileo-a2a` uses OpenTelemetry auto-instrumentation to wrap both client and server methods in the `a2a-sdk`. When a client agent sends a message to a server agent, trace context is injected into the A2A request metadata. The server-side instrumentation extracts this context and creates child spans under the same trace, giving you a single distributed trace across all agents.

A2A's `context_id` is automatically mapped to `session.id`, so all interactions within the same conversation are grouped into a Galileo session.

## Configuration

`A2AInstrumentor.instrument()` accepts the following parameters:

| Parameter | Type | Description |
|-----------|------|-------------|
| `tracer_provider` | `TracerProvider` | The OTel tracer provider to use for creating spans. Must have the `GalileoSpanProcessor` registered. |
| `agent_name` | `str` (optional) | A human-readable name for this agent. Appears as `gen_ai.agent.name` in span attributes. |

Environment variables for Galileo configuration:

| Variable | Description |
|----------|-------------|
| `GALILEO_API_KEY` | Your [Galileo API key](https://app.galileo.ai/settings/api-keys) |
| `GALILEO_PROJECT` | The Galileo project to log traces to |
| `GALILEO_LOG_STREAM` | The Log stream within the project |

<Note>
**AGNTCY compatibility:** `galileo-a2a` is compatible with the [AGNTCY Observe SDK](https://docs.agntcy.org/pages/agntcy/observe-sdk/). If an incoming request uses the AGNTCY `observe` metadata key instead of `galileo_observe`, the instrumentor will extract trace context from it. Galileo's key takes precedence when both are present.
</Note>

## Span attributes

{/*<!-- markdownlint-disable MD013 -->*/}

<Expandable title="All span attributes created by galileo-a2a">

| Attribute | Description |
|-----------|-------------|
| `gen_ai.operation.name` | The operation type (`invoke_agent` for client, `process_agent_request` for server) |
| `gen_ai.system` | Always `"a2a"` |
| `gen_ai.agent.name` | The agent name (if provided to `instrument()`) |
| `gen_ai.input.messages` | JSON-encoded input message text |
| `gen_ai.output.messages` | JSON-encoded output message text |
| `gen_ai.response.finish_reasons` | Finish reason based on task state |
| `a2a.rpc.method` | The A2A RPC method (`SendMessage`, `GetTask`, etc.) |
| `a2a.task.id` | The A2A task identifier |
| `a2a.task.state` | The final task state (`completed`, `failed`, etc.) |
| `a2a.context_id` / `session.id` | The conversation context ID |

</Expandable>

{/*<!-- markdownlint-enable MD013 -->*/}

## Next steps

<CardGroup cols={2}>
<Card title="OpenTelemetry overview" icon="code" horizontal href="/sdk-api/third-party-integrations/opentelemetry-and-openinference">
    Learn more about Galileo's OpenTelemetry integration.
</Card>
<Card title="Sessions" icon="messages" horizontal href="/concepts/logging/sessions/sessions-overview">
    Learn how sessions group related traces together.
</Card>
<Card title="Distributed tracing" icon="diagram-project" horizontal href="/sdk-api/logging/distributed-tracing">
    Learn about distributed tracing concepts in Galileo.
</Card>
<Card title="Agentic metrics" icon="chart-line" horizontal href="/concepts/metrics/agentic/agentic-overview">
    Evaluate your multi-agent systems with agentic AI metrics.
</Card>
</CardGroup>
