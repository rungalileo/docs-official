```python Python (Beta)
from galileo import Dataset

# Get a dataset by name
dataset = Dataset.get(name="countries")

# Get a dataset by ID
dataset = Dataset.get(
    id="dataset-id"
)

# Get its content
dataset.get_content()
```
