```python Python
# Step 1: Set up Elasticsearch index and data
document_store = setup_elasticsearch()

# Step 2: Compile the agent and its workflow
rag_agent_graph = setup_agent_and_graph(document_store)

# Step 3: Start a Q&A session
print("\n--- Starting Q&A Session ---")
session_id = f"session-{int(time.time())}"

# Example questions
print("Question: What does our health insurance cover?")
answer = ask_question(rag_agent_graph, "What does our health insurance cover?", session_id)
print(f"Answer: {answer}\n")

print("Question: How many vacation days do new employees get?")
answer = ask_question(rag_agent_graph, "How many vacation days do new employees get?", session_id)
print(f"Answer: {answer}\n") 
```