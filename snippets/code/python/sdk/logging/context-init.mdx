```python Python
from galileo.__future__ import LogStream

log_stream = LogStream.get(name="my-log-stream", project_name="my-project")

# Or create a new one
log_stream = LogStream(name="my-log-stream", project_name="my-project")
log_stream.create()
```
