```python Python
def create_prompt(
    name: str,
    template: List[Message]
) -> Prompt:
    """
    Creates a new prompt .

    Parameters:
        name: The name of the prompt
        template: List of Message objects defining the prompt

    Returns:
        The created Prompt object.
    """
    pass
```
