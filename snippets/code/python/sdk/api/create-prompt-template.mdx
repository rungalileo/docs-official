```python Python
def create_prompt(
    name: str,
    messages: List[Message]
) -> Prompt:
    """
    Creates a new prompt .

    Parameters:
        name: The name of the prompt
        messages: List of Message objects defining the prompt

    Returns:
        The created Prompt object.
    """
    pass
```
