
```python Python
from galileo.datasets import get_dataset

# Get a dataset by name
dataset = get_dataset(
    name="countries",
    project="my-project",
)

# Get a dataset by ID
dataset = get_dataset(
    id="dataset-id",
    project="my-project",
)

# Get a dataset with its content
dataset = get_dataset(
    name="countries", 
    with_content=True,
    project="my-project",
) 
```