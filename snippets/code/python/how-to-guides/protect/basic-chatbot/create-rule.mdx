```python create_central_stage.py
# Create a rule for toxicity
toxicity_rule = Rule(
    metric="input_toxicity",        # metric to evaluate
    operator=RuleOperator.gt,       # trigger if score is greater than
    target_value=0.01               # threshold for triggering
)
```