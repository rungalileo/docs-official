```python app.py
# Check the guardrail status
if guardrail_response.status == ExecutionStatus.triggered:
    # If the guardrail is triggered, print the action result
    print(guardrail_response.action_result["value"])
    # Skip the LLM call and end the conversation
    break
```