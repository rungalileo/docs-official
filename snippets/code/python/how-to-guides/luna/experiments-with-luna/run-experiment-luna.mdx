```python Python
results = run_experiment(
    "geography-experiment-luna", # New name
    dataset=dataset,
    function=llm_call,
    metrics=[GalileoScorers.prompt_injection_luna], # Use the Luna-2 metric
    project=os.environ["GALILEO_PROJECT"]
)
```
