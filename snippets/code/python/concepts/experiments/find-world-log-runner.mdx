```python Python {2}
# The experiment's custom function
@log(span_type="workflow")
def llm_call(world_name: str) -> str:
    """Simulate invoking an LLM"""

    response = describe_world(world_name=world_name)
    print(f"Model Response: {response}")

    return response
```
