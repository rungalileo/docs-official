```python main.py
import os
from dotenv import load_dotenv

# Galileo dependencies
from galileo import galileo_context
from galileo.handlers.langchain import GalileoCallback

# LangChain and LangGraph dependencies
from langgraph.prebuilt import create_react_agent

# Load `.env` variables
load_dotenv()

# For more complex apps, you can define your assistant elsewhere and import it here.
# You can also provide tools! Galileo will add Spans for tool calls into your session.
simple_assistant = create_react_agent(
    name="simple_assistant",
    model="openai:o3-mini",  # you can choose any OpenAI model here
    prompt="You are a friendly assistant that answers the user's questions",
    tools=[] 
)
```