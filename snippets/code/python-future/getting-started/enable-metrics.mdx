```python Python (Beta)
from galileo.__future__ import LogStream, Metric

# Get the log stream
log_stream = LogStream.get(
    name="MyFirstLogStream",
    project_name="MyFirstEvaluation"
)

# Enable context adherence metric
log_stream.set_metrics([Metric.scorers.context_adherence])
```
