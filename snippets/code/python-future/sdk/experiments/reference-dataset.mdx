```python Python (Beta)
from galileo.__future__ import Message, MessageRole

Message(role=MessageRole.user,
        content="""
        Plan a {{ days }}-day travel itinerary
        for a trip to {{ city }}.
        """)
```
