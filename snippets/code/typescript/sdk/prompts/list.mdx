```typescript TypeScript
import { getPrompts } from "galileo";

// Search list of prompts by name
const templates = await getPrompts(name="story");

// Log the template names
templates.forEach((template) => console.log(template.name));
```
