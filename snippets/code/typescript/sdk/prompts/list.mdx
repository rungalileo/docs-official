```typescript TypeScript
import { getPromptTemplates } from "galileo";

// Get all prompt templates in a project
const templates = await getPromptTemplates('my-project');

// Log the template names
templates.forEach((template) => console.log(template.name));
```
