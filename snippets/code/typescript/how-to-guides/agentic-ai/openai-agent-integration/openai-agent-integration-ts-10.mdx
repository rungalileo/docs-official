```typescript TypeScript
// Input is changed to non-homework question
async function main() {
  let result = await Runner.run(triageAgent, "What is the meaning of life?");
  console.log(result.finalOutput);
}
```
