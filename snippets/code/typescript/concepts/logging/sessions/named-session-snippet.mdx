```python main.py {7}

# Our LangChain Runnable config is the same as before:
agent_config = RunnableConfig(callbacks=[GalileoCallback()])

# Here's where we make the first change. This will start the session and log our
# Traces and Spans. 'start_session' also takes a few optional args, show below:
galileo_context.start_session(name="My Session Name")
```