```typescript TypeScript
import { enableMetrics, GalileoScorers } from "galileo";

// Enable metrics
await enableMetrics({
    projectName: "MyProject",
    logStreamName: "MyLogStream",
    metrics: [GalileoScorers.ContextAdherence]
});
```
