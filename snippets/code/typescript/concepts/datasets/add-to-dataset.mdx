```typescript TypeScript
import { getDataset } from "galileo";

async function addToDataset() {
  // Get the dataset (version 3, or latest if not specified)
  const dataset = await getDataset({ name: "countries" });

  // Add new samples
  // Note: The exact API for adding samples may differ in the final implementation
  await dataset.insert([
    {
      input: "Which continent is Morocco in?",
      output: "Africa",
    },
    {
      input: "Which continent is Australia in?",
      output: "Oceania",
    },
  ]);
}
```
