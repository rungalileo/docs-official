```python Python
def search_documents(query: str, user_context: dict):
    # Start trace for document retrieval
    trace = logger.start_trace(
        input=query,
        name="RAG Document Retrieval",
        metadata={
            "user_role": user_context.get("role"),
            "search_type": "semantic",
            "embedding_model": "text-embedding-ada-002",
            "max_results": "10"
        }
    )
    
    # Perform search...
    results = vector_search(query)
    
    # Conclude with search results
    logger.conclude(
        output=results,
        duration_ns=search_duration,
        metadata={
            "results_count": len(results),
            "top_similarity_score": max_similarity,
            "search_latency_ms": search_duration / 1_000_000
        }
    )
```

```typescript TypeScript
function searchDocuments(query: string, userContext: Record<string, any>) {
    // Start trace for document retrieval
    const trace = logger.startTrace({
        input: query,
        name: "RAG Document Retrieval",
        metadata: {
            user_role: userContext.role,
            search_type: "semantic",
            embedding_model: "text-embedding-ada-002",
            max_results: "10"
        }
    });
    
    // Perform search...
    const results = vectorSearch(query);
    
    // Conclude with search results
    logger.conclude({
        output: results,
        durationNs: searchDuration,
        metadata: {
            results_count: results.length,
            top_similarity_score: maxSimilarity,
            search_latency_ms: searchDuration / 1_000_000
        }
    });
}
```