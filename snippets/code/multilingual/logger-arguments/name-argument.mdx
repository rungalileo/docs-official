```python Python
# ✅ Good: Descriptive, action-oriented names
logger.start_trace(
    input=user_query,
    name="RAG Document Retrieval for Medical Query"
)

# ✅ Good: Hierarchical naming for complex workflows
logger.start_trace(
    input=user_query,
    name="Customer Support - Payment Issue Resolution"
)

# ❌ Avoid: Generic or unclear names
logger.start_trace(
    input=user_query,
    name="Query"
)
```

```typescript TypeScript
// ✅ Good: Descriptive, action-oriented names
logger.startTrace({
    input: userQuery,
    name: "RAG Document Retrieval for Medical Query"
});

// ✅ Good: Hierarchical naming for complex workflows
logger.startTrace({
    input: userQuery,
    name: "Customer Support - Payment Issue Resolution"
});

// ❌ Avoid: Generic or unclear names
logger.startTrace({
    input: userQuery,
    name: "Query"
});
```

