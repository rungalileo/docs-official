```bash Bash
curl -X PUT "${GALILEO_API_URL}/integrations/custom" \
  -H "Content-Type: application/json" \
  -H "Galileo-API-Key: ${GALILEO_API_KEY}" \
  -d '{
    "authentication_type": "oauth2",
    "models": ["custom-model"],
    "endpoint": "https://api.provider.com/v1",
    "token": "{\"client_id\": \"my_client\", \"client_secret\": \"my_secret\"}",
    "oauth2_token_url": "https://api.provider.com/oauth2/token",
    "authentication_scope": "inference"
  }'
```
