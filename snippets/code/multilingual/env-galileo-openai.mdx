```ini .env
# Galileo + OpenAI Environment Configuration
# Save this file as `.env` in your project root and replace placeholder values.

# ----------------- Galileo configuration (required) -----------------

# Galileo Console URL (choose one)
# - Hosted Galileo (default / free tier): keep this line
GALILEO_CONSOLE_URL="https://app.galileo.ai"

# - Custom Deployment (enterprise): uncomment this line and comment the above
#   Example value: "https://console.galileo.yourcompany.com"
# GALILEO_CONSOLE_URL="your-galileo-console-url"

# Galileo API key
# - Get this from the Galileo Console → API Keys page:
#   https://app.galileo.ai/settings/api-keys   (hosted)
#   <your-console-base-url>/settings/api-keys  (custom)
# - If no key exists, click "+ Create new key"
# - Save this key securely — it will only be shown once
# - If you already saved a key earlier, reuse it; if lost, create a new one
GALILEO_API_KEY="your-galileo-api-key"

# Galileo project name
GALILEO_PROJECT="your-galileo-project-name"

# Galileo log stream (auto-created if it doesn’t exist)
GALILEO_LOG_STREAM="your-galileo-log-stream"

# ----------------- OpenAI configuration (required) -----------------

# OpenAI API key (from your OpenAI provider portal)
OPENAI_API_KEY="your-openai-api-key"

# ----------------- OpenAI configuration (optional) -----------------

# OpenAI base URL (only if not using the default public API)
# OPENAI_BASE_URL="your-openai-base-url"

# OpenAI organization ID (only if your provider requires it)
# OPENAI_ORGANIZATION="your-openai-organization"
```