export const Screenshot = ({ imageUrl, alt, caption, placeholder }) => {
  if (imageUrl) {
    return (
      <div>
        <img src={`${imageUrl}`} alt={alt || "Screenshot"} />
        {caption && <p style={{ textAlign: "center", fontSize: "0.875rem", color: "#6b7280", marginTop: "0.5rem" }}>{caption}</p>}
      </div>
    );
  }
  
  return (
    <div>
      <div style={{ 
        backgroundColor: "#f8fafc",
        borderRadius: "8px",
        border: "2px dashed #e2e8f0",
        padding: "2rem",
        textAlign: "center",
        color: "#64748b",
        minHeight: "200px",
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center",
        margin: "1rem 0"
      }}>
        <div style={{ fontSize: "2rem", marginBottom: "1rem" }}>📷</div>
        <div>{placeholder || "Screenshot placeholder"}</div>
      </div>
      {caption && <p style={{ textAlign: "center", fontSize: "0.875rem", color: "#6b7280", marginTop: "0.5rem" }}>{caption}</p>}
    </div>
  );
} 