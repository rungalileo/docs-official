export const ImportantNotes = ({ children }) => {
  return (
    <div style={{
      backgroundColor: '#fff3cd',
      border: '1px solid #ffeaa7',
      borderRadius: '0.5rem',
      padding: '1rem',
      margin: '1rem 0',
      borderLeft: '4px solid #f39c12'
    }}>
      <div style={{
        fontWeight: 'bold',
        color: '#856404',
        marginBottom: '0.5rem',
        fontSize: '1.1rem'
      }}>
        ⚠️ Important Notes:
      </div>
      <div style={{
        color: '#856404',
        lineHeight: '1.6'
      }}>
        {children}
      </div>
    </div>
  );
};
