import GalileoAzureOpenAIEnvVars from "/snippets/code/multilingual/env-galileo-azure-openai.mdx"
import SnippetGettingStartedAzureOpenAITypeScript from "/snippets/code/typescript/getting-started/getting-started-azure-openai.mdx";
import LLMOutputNoContext from "/snippets/content/get-started/quickstart/llm-output-no-context.mdx";

## Create your TypeScript project

<Steps>

<Step title="Install dependencies">

Install the **Galileo SDK** using the following command in your terminal:

<CodeGroup>
```bash Terminal
npm install galileo tsx
```
</CodeGroup>

</Step>

<Step title="Set up your environment variables">

Create an `.env` file in your project folder, and add your [Galileo API key](https://app.galileo.ai/settings/api-keys), project and Log stream name, as well as your Azure OpenAI credentials.
<CodeGroup>
<GalileoAzureOpenAIEnvVars/>
</CodeGroup>

<Note>
This assumes you are using a free Galileo account. If you are using a custom deployment, then you will also need to add the URL of your Galileo Console:

```ini .env
GALILEO_CONSOLE_URL=your-Galileo-console-URL
```
</Note>
</Step>
<Step title="Create your application code">
Create a file called `app.ts` and add the following code:

<CodeGroup>
  <SnippetGettingStartedAzureOpenAITypeScript />
</CodeGroup>

<Note>
This code defaults to using GPT-4o mini. If you want to use a different model, update the `model` in the call to `chat.completions.create`. For this get started guide, use the smallest, oldest model possible to get the best results - you want to see the AI behaving badly!
</Note>

</Step>
<Step title="Run your application">

Run your application using the following command in your terminal:

<CodeGroup>
```bash Terminal
npx tsx app.ts
```
</CodeGroup>

</Step>
<Step title="View the results in your terminal">

You can see the model's output in your terminal:

<LLMOutputNoContext />

As you can see from the output, the AI has hallucinated! Galileo is an AI reliability platform, not a financial technology platform.

</Step>
<Step title="See the trace in Galileo">

Open the Log streams for your project in the Galileo console. You will see the trace.

![The first trace in Galileo](/snippets/content/get-started/quickstart/first-trace.webp)

Select the trace, then navigate to the **Messages** tab to see details of the trace.

![The first trace in Galileo](/snippets/content/get-started/quickstart/first-trace-messages.webp)
</Step>
</Steps>
