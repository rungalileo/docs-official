---
title: Visual Fidelity
description: Evaluate whether a generated image in an LLM span complies with every applicable provided brand rule based on visible evidence
---

import { Pill } from "/snippets/components/pill.mdx";
import { DefinitionCard } from "/snippets/components/definition-card.mdx";
import { MetricWhenToUse } from "/snippets/components/metric-when-to-use.mdx";
import { Scale } from "/snippets/components/scale.mdx";
import { BooleanConfusionMatrix, BooleanClassificationReport } from "/snippets/components/confusion-matrix.jsx";

<DefinitionCard>
  <strong>Visual Fidelity</strong> is a binary metric that evaluates whether a generated image in an LLM span satisfies every applicable provided brand rule, based solely on visible evidence in the image.
</DefinitionCard>

The Visual Fidelity metric is a rule-adherence check: using only visible evidence from the image and the provided brand rules, the evaluator determines whether the image satisfies every applicable rule. The metric is grounded in explicit rule compliance rather than pure aesthetics, prompt reconstruction, or any separate image-quality standard not written in the rules.

This is a **boolean** metric, returning a confidence score that the generated image is compliant. The score ranges from 0% (no confidence the image is compliant) to 100% (complete confidence that the image is compliant).

## Visual Fidelity at a glance

| Property                       | Description                                    |
| :----------------------------- | :--------------------------------------------- |
| **Name**                       | Visual Fidelity                                |
| **Category**                   | Multimodal Quality                             |
| **Metric Level**               | LLM Span                                       |
| **LLM-as-a-judge Support**     | ✅                                             |
| **Luna Support**               | ❌                                             |
| **Protect Runtime Protection** | ❌                                             |
| **Value Type**                 | Boolean shown as a percentage confidence score |

## Score interpretation

<Scale
  low="0"
  mid="50%"
  high="100%"
  lowLabel="Non-Compliant"
  midLabel="Borderline"
  highLabel="Compliant"
  lowDescription="One or more applicable provided rules are violated based on visible evidence in the image"
  highDescription="All applicable provided rules pass based on visible evidence in the image"
/>

| Score | Label         | Meaning                                                                                   |
| :---- | :------------ | :---------------------------------------------------------------------------------------- |
| **0** | Non-Compliant | One or more applicable provided rules are violated based on visible evidence in the image |
| **1** | Compliant     | All applicable provided rules pass based on visible evidence in the image                 |

## How to interpret Visual Fidelity

Visual Fidelity is a rule-adherence check over a generated image:

- If the image is <Pill label="Compliant" />, all applicable provided rules pass based on visible evidence in the image.
- If the image is <Pill label="Non-Compliant" />, one or more applicable rules are violated based on visible evidence in the image.

## When to use this metric

<MetricWhenToUse
  description="Visual Fidelity is useful whenever your pipeline generates images that must conform to explicit brand, style, or content rules"
  useCases={[
    {
      title: "Brand and style guideline enforcement",
      description: "Verify that AI-generated marketing or product images conform to color palettes, logo placement, typography, and layout rules defined in your brand guidelines.",
    },
    {
      title: "Content policy compliance",
      description: "Confirm that generated images follow explicit content rules, such as prohibited objects, required safety symbols, or mandatory disclosures.",
    },
    {
      title: "Template and layout adherence",
      description: "Check that generated images match required compositional rules, such as text zones, bleed areas, or safe-region constraints.",
    },
    {
      title: "Regression testing for image generation pipelines",
      description: "Detect regressions when model versions or prompts change by re-running the same rule set against newly generated images.",
    },
  ]}
/>

## Example scenario

<Card>
  <div style={{display: 'flex', alignItems: 'center', gap: '0.5rem', marginBottom: '0.75rem'}}>
    <div style={{fontSize: '1.25rem', color: 'var(--primary-color)'}}>
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
        <path d="M12 22c5.523 0 10-4.477 10-10S17.523 2 12 2 2 6.477 2 12s4.477 10 10 10z"></path>
        <path d="m9 12 2 2 4-4"></path>
      </svg>
    </div>
    <h3 style={{margin: 0, fontSize: '1.25rem', fontWeight: '600'}}>Brand rules compliance for a generated banner</h3>
  </div>

<div style={{ marginTop: "1rem", paddingTop: "0.75rem", borderTop: "1px solid rgba(209, 213, 219, 0.33)" }}>
  <strong>Provided rules:</strong> “Logo must appear in the top-left”, “Primary color must be #E35454”, “No competitor logos”.
</div>

<div style={{ marginTop: "0.75rem", paddingTop: "0.75rem", borderTop: "1px solid rgba(209, 213, 219, 0.33)" }}>
  <strong>
    Compliant (<Pill label="Compliant" />
    ):
  </strong>{" "}
  The generated banner visibly satisfies each applicable rule (logo placement, correct primary color usage, no prohibited content).
</div>

<div style={{ marginTop: "0.75rem", paddingTop: "0.75rem", borderTop: "1px solid rgba(209, 213, 219, 0.33)" }}>
  <strong>
    Non-Compliant (<Pill label="Non-Compliant" />
    ):
  </strong>{" "}
  The logo is missing or misplaced, the primary color rule is violated, or prohibited content is present — any single rule violation fails the metric.
</div>

</Card>

## Inputs considered

The evaluator examines the following when available:

- The generated image produced by the LLM span (output image)
- The set of provided brand or content rules that apply to the image

Only rules that are **applicable** to the generated image are evaluated; inapplicable rules are skipped and do not affect the score. Compliance is determined solely from what is **visually observable** — the evaluator does not infer intent or reconstruct the original prompt.

## Calculation method

Visual Fidelity is computed through a multi-step process:

<Steps>
  <Step title="Rule scoping">Determine which provided rules are applicable to the generated image and should be evaluated.</Step>
  <Step title="Visible-evidence evaluation">Using only visible evidence in the image, evaluate each applicable rule as pass or fail. The evaluator does not reconstruct prompts or apply any external image-quality standard.</Step>
  <Step title="All-rules decision">
    Return <Pill label="Compliant" /> if and only if all applicable rules pass. Otherwise return <Pill label="Non-Compliant" />.
  </Step>
</Steps>

<Note>This metric is typically computed by prompting an LLM with access to the generated image and the provided rules, which may require additional LLM calls to compute and can impact usage and billing.</Note>

## What constitutes compliant (1)

- Every applicable rule in the provided rule set passes based on visible evidence in the image
- No rule violation is detectable from the image alone, regardless of other quality dimensions

## What constitutes non-compliant (0)

- At least one applicable rule is violated by visible evidence in the image
- Examples: a prohibited color is used, a required logo is missing, restricted content is present, a mandatory text zone is empty

## Best practices

<CardGroup cols={2}>
  <Card title="Write observable rules" icon="eye">
    Each rule should describe something that can be confirmed or denied from visual inspection alone. Avoid rules that require knowledge of the generation prompt or model internals.
  </Card>
  <Card title="Keep rules atomic" icon="list-check">
    Express one constraint per rule so that a failing rule identifies a specific violation rather than a bundle of requirements.
  </Card>
  <Card title="Version your rule sets" icon="code-branch">
    Treat brand rule sets as versioned artifacts so changes in guidelines can be tracked and their effect on compliance scores can be measured.
  </Card>
  <Card title="Combine with Visual Quality" icon="layer-group">
    Use Visual Quality on input images and Visual Fidelity on generated images to cover both ends of a multimodal pipeline.
  </Card>
</CardGroup>

## Performance Benchmarks

We will publish Visual Fidelity benchmark results once an internal evaluation dataset and gold labels are available.

<Note>Benchmarks for this metric are not yet published. When available, this section will include model-level performance and a classification report with a normalized confusion matrix.</Note>

<Note type="warning">Placeholder benchmarks below are rendered only as a template. Replace the table values and the classification report string with your real benchmark outputs.</Note>

| Model             | F1 (True) |
| :---------------- | :-------: |
| GPT-4.1           |   0.79    |
| Claude Sonnet 4.6 |   0.76    |
| Gemini 3.1 Flash  |   0.81    |

### GPT-4.1 Classification Report

<BooleanClassificationReport
  report={`            precision    recall  f1-score   support

False 0.0000 0.0000 0.0000 1
True 0.0000 0.0000 0.0000 1

accuracy 0.0000 2
macro avg 0.0000 0.0000 0.0000 2
weighted avg 0.0000 0.0000 0.0000 2`}
negativeLabel="Non-Compliant"
positiveLabel="Compliant"
negativeClass="False"
positiveClass="True"
/>

## Related Resources

If you would like to dive deeper or start implementing Visual Fidelity, check out the following resources:

### Examples

- [Visual Fidelity Examples](https://app.galileo.ai) - Log in and explore the "Visual Fidelity" Log Stream in the "Preset Metric Examples" Project to see this metric in action.

### Related Concepts

- [Visual Quality](/concepts/metrics/multimodal-quality/visual-quality)
- [Interruption Detection](/concepts/metrics/multimodal-quality/interruption-detection)
